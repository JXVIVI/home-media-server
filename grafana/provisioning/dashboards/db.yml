apiVersion: 1
providers:
  - name: 'Dashboards'
    type: file
    options:
      path: /var/lib/grafana/dashboards
    inputs:
      - name: DS_PROMETHEUS
        type: datasource
        pluginId: prometheus
        value: Prometheus

