apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    url: http://127.0.0.1:9090
    isDefault: true
    uid: prometheus  # This helps resolve the ${DS_PROMETHEUS} error
    jsonData:
      httpMethod: POST
      manageAlerts: true
